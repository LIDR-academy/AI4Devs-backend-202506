# Prompt Endpoints Generation

Quiero que implementes dos endpoints nuevos para gestionar candidatos en una UI estilo kanban. El proyecto es un backend existente con Node.js + TypeScript + Express + Prisma (PostgreSQL). Mantén el estilo de código y convenciones actuales.

Objetivo

1. GET /positions/:id/candidates
2. PUT /candidates/:id/stage
